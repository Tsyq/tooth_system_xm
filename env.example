# ============================================
# 牙科预约管理系统 - 环境变量配置示例文件
# ============================================
# 使用说明：
# 1. 复制此文件为 .env
# 2. 根据实际情况修改各项配置
# 3. .env 文件不要提交到 Git 仓库（已在 .gitignore 中配置）

# ============================================
# Django 基础配置
# ============================================
# Django密钥（生产环境必须修改！）
# 生成方式：python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-8^9bnj&3)2an&iqs^lt*up-1^c+qhpknmjiwlt@m9$^i-a=sl!

# 调试模式（开发环境设为True，生产环境必须设为False）
DEBUG=True

# 允许的主机（多个用逗号分隔）
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================
# 数据库配置（MySQL）
# ============================================
# 数据库名称
DB_NAME=tooth_system

# 数据库用户名
DB_USER=root

# 数据库密码（必须修改为您的MySQL密码）
DB_PASSWORD=123456

# 数据库主机地址
DB_HOST=localhost

# 数据库端口
DB_PORT=3306

# ============================================
# CORS 跨域配置
# ============================================
# 允许的前端地址（多个用逗号分隔）
# 开发环境常见端口：
# - React: http://localhost:3000
# - Vite: http://localhost:5173
# - Vue CLI: http://localhost:8080
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

# ============================================
# 可选配置（如需要可取消注释）
# ============================================
# JWT密钥（如不设置，将使用SECRET_KEY）
# JWT_SECRET_KEY=your-jwt-secret-key

# 文件上传大小限制（MB）
# FILE_UPLOAD_MAX_MEMORY_SIZE=5

